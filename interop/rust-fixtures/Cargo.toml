[package]
name = "postino-fixtures"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
postcard = { version = "1.1", features = ["alloc", "use-std"] }
serde = { version = "1", features = ["derive"] }
