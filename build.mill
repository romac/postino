package build

import mill.*, scalalib.*
import mill.api.BuildCtx
import mill.javalib.publish.*
import mill.scalalib.scalafmt.ScalafmtModule

trait PostinoPublishModule extends ScalaModule with mill.javalib.SonatypeCentralPublishModule:
  def publishVersion: T[String] = Task.Input:
    Task.env.getOrElse("POSTINO_VERSION", "0.1.0-M1")

  def postinoGroupId: T[String] = Task.Input:
    Task.env.getOrElse("POSTINO_GROUP_ID", "me.romac")

  def pomSettings: T[PomSettings] = Task:
    PomSettings(
      description = "Scala 3 implementation of the Rust postcard 1.x wire format.",
      organization = postinoGroupId(),
      url = "https://github.com/romac/postino",
      licenses = Seq(License.`Apache-2.0`),
      versionControl = VersionControl(
        browsableRepository = Some("https://github.com/romac/postino"),
        connection = Some("scm:git:https://github.com/romac/postino.git"),
        developerConnection = Some("scm:git:ssh://git@github.com/romac/postino.git"),
      ),
      developers = Seq(
        Developer(id = "romac", name = "romac")
      ),
    )

  def versionScheme: T[Option[VersionScheme]] = Task:
    Some(VersionScheme.EarlySemVer)

object `package` extends PostinoPublishModule with ScalafmtModule:
  outer =>

  override def artifactName = "postino"

  override def scalaVersion = "3.8.3"

  override def scalacOptions = Seq(
    "-deprecation",
    "-feature",
    "-unchecked",
    "-new-syntax",
    "-Wunused:imports",
  )

  def fmt() = Task.Command:
    mill.scalalib.scalafmt.ScalafmtWorkerModule.worker().reformat(
      filesToFormat(sources()) ++
      filesToFormat(test.sources()) ++
        filesToFormat(postinoScodec.sources()) ++
        filesToFormat(postinoScodec.test.sources()) ++
        filesToFormat(postinoCirce.sources()) ++
        filesToFormat(postinoCirce.test.sources()),
      scalafmtConfig().head,
    )

  def interopTest() = Task.Command:
    val fixturePath = BuildCtx.workspaceRoot / "interop/fixtures/postcard-1.1.3.hex"
    println("Generating Rust postcard fixtures with Cargo...")

    val cargoResult =
      os.proc(
        "cargo",
        "run",
        "--quiet",
        "--manifest-path",
        "interop/rust-fixtures/Cargo.toml",
      ).call(
        cwd = BuildCtx.workspaceRoot,
        stdout = os.Pipe,
        stderr = os.Pipe,
        check = false,
      )

    if cargoResult.exitCode != 0 then
      Task.fail(
        s"Rust fixture generator failed with exit code ${cargoResult.exitCode}:\n${cargoResult.err.text()}"
      )

    val expected = canonicalFixture(os.read(fixturePath))
    val actual   = canonicalFixture(cargoResult.out.text())

    if actual != expected then
      os.write.over(Task.dest / "expected.hex", expected)
      os.write.over(Task.dest / "actual.hex", actual)
      Task.fail(
        s"Rust postcard fixtures differ. See ${Task.dest / "expected.hex"} and ${Task.dest / "actual.hex"}."
      )

    val fixtureCount = actual.linesIterator.count(_.nonEmpty)
    println(s"Compared $fixtureCount Rust postcard fixtures against $fixturePath")
    println("Rust postcard compatibility check passed.")

  private def canonicalFixture(text: String): String =
    text.replace("\r\n", "\n").stripTrailing + "\n"

  object test extends ScalaTests with TestModule.Munit:
    def rustFixtureResources = Task.Sources(BuildCtx.workspaceRoot / "interop/fixtures")

    override def resources = Task {
      super.resources() ++ rustFixtureResources()
    }

    override def mvnDeps = Seq(
      mvn"org.scalameta::munit:1.2.1",
    )

  object postinoScodec extends PostinoPublishModule:
    override def artifactName = "postino-scodec"
    override def scalaVersion = outer.scalaVersion()
    override def scalacOptions = outer.scalacOptions()
    override def publishVersion = outer.publishVersion()
    override def postinoGroupId = outer.postinoGroupId()
    override def sources = Task.Sources(BuildCtx.workspaceRoot / "postino-scodec/src")
    override def moduleDeps = Seq(outer)

    override def mvnDeps = Seq(
      mvn"org.scodec::scodec-core:2.3.3",
    )

    object test extends ScalaTests with TestModule.Munit:
      override def sources = Task.Sources(BuildCtx.workspaceRoot / "postino-scodec/test/src")

      override def mvnDeps = Seq(
        mvn"org.scalameta::munit:1.2.1",
      )

  object postinoCirce extends PostinoPublishModule:
    override def artifactName = "postino-circe"
    override def scalaVersion = outer.scalaVersion()
    override def scalacOptions = outer.scalacOptions()
    override def publishVersion = outer.publishVersion()
    override def postinoGroupId = outer.postinoGroupId()
    override def sources = Task.Sources(BuildCtx.workspaceRoot / "postino-circe/src")
    override def moduleDeps = Seq(outer)

    override def mvnDeps = Seq(
      mvn"io.circe::circe-core:0.14.15",
    )

    object test extends ScalaTests with TestModule.Munit:
      override def sources = Task.Sources(BuildCtx.workspaceRoot / "postino-circe/test/src")

      override def mvnDeps = Seq(
        mvn"org.scalameta::munit:1.2.1",
      )
